# Use Promptfoo and OpenCode

Promptfoo owns evaluation, result storage, skill-use assertions, and comparisons. OpenCode owns model routing, agent execution, and automatic project-local skill discovery. This avoids maintaining a custom agent harness while preserving the real runtime behavior under test.
